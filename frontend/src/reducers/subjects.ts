const reducer = (state = {}, action: any) => {
    switch(action.type) {
        case 'FETCH_ALL':
            return state;
        default:
            return state;
    }
}

export default reducer;